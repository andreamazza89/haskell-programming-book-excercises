-- Reader is not a new concept, but a special mention to the Functor/Applicative/Monad for the function type (-> a)

-- The special thing about it is that it gives us a pattern to share some data across a number of functions without
-- having to explicitly pass it in everywhere as an argument.
